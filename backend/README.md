# Backend Bootstrap

현재 단계는 Spring Boot 백엔드 기본 진입점과 공통 설정만 구성되어 있습니다.

- `pom.xml`: Java 21 + Spring Boot 3.5.5 최소 의존성
- `application.yml`: `dev`/`prod` 프로필 분리
- `CorsConfig`: 프론트 로컬 개발 서버 허용
- `ApiResponse`: 공통 응답 래퍼 초안
